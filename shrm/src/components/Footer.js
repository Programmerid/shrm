import React from 'react';

const Footer = () => {
    return (
        <footer>
            {/* Footer content */}
            <p>&copy; {new Date().getFullYear()} My React App</p>
        </footer>
    );
}

export default Footer;